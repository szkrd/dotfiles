# multiple services
export CHAOS_CRASH_ENABLED="false"

# disable export service runners
export DISABLE_SINGLE_INSTANCE_TASKS="true"

# filer can fall back to an url prefix for 404 images
export NOT_FOUND_FALLBACK="http://foobarfiler-staging.XXXXX.XXX"

# this will conflict with plano! (but I'm not using that very often)
export AWS_DEFAULT_REGION="us-east-1"
export AWS_PROFILE="XXXX"
